h2 Thêm sách
form(action="/books/create", method="POST")
  lable(for="book") Tiêu đề
  span &nbsp
  input#book(type='text', name="title")
  span &nbsp
  button Tạo
  
h2 Create ToDo
form(action="/books/create", method="POST")
  lable(for="name") Name
  input(id="name", name="title")
  button Create  