extends ../layout/common.pug
block content
  .container
    h2 Thêm sách
    form(action="/books/create", method="POST")
      .form-group.row
        lable(for="book", class="col-sm-2 col-form-label") Tiêu đề
        .col-sm-10
          input#book(type='text', name="title", class="form-control-plaintext")

      div
        lable(for="mota") Mô tả
        span &nbsp
        input#mota(type='text', name="description")
      button Tạo
