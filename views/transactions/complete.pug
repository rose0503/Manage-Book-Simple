extends ../layout/common.pug
block content
  .container.mt-2
    .d-flex.justify-content-between
      h2.mb-4  Thuê sách      
      a.h-25(href='/transactions', class="btn btn-primary btn-md", role="button", aria-pressed="true") Quay lại
    
    if errors
      each error in errors
        .alert.alert-danger= error
      